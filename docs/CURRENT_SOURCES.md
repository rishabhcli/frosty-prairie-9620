# Current Documentation Sources

Verified 2026-07-20 against live/primary sources before implementation and re-checked
2026-07-29 before submission work. Where current official rules differ from PLAN.md's
assumptions, the decision column states what this build actually does and why.

| Area | Source | Access date | Version / fact | Decision |
|---|---|---|---|---|
| Hackathon rules | [cockroachdb-ai.devpost.com](https://cockroachdb-ai.devpost.com/) | 2026-07-29 | Deadline **August 18, 2026, 5:00pm EDT**. Video <3 min, public on YouTube/Vimeo. Public OSS repo + license required. Must use ≥2 of {Cloud Managed MCP Server, Distributed Vector Indexing, ccloud CLI, Agent Skills Repo}. Must use ≥1 AWS service (Bedrock, Lambda, ECS/EKS, S3, SageMaker, Bedrock Agents). Judging: Agentic Memory Design, Technical Implementation, Real-World Impact, Production Readiness, Creativity & Originality. | This release demonstrates only **Distributed Vector Indexing** as a qualifying CockroachDB feature. Managed MCP has not been run, no second qualifying tool is demonstrated, Bedrock is fixture-backed, and no AWS deployment exists. It is therefore not ready for final submission. |
| CockroachDB vector indexes | [cockroachlabs.com/docs/stable/vector-indexes](https://www.cockroachlabs.com/docs/stable/vector-indexes) | 2026-07-20 | Introduced in v25.2 (C-SPANN algorithm). Requires `SET CLUSTER SETTING feature.vector_index.enabled = true;`. Syntax: `embedding VECTOR(n)` column + `VECTOR INDEX (prefix_cols, embedding)` or `VECTOR INDEX idx (embedding vector_cosine_ops)`. **Creating a vector index via backfill (ALTER TABLE ... ADD) disables mutations on the table until backfill completes** — this build always defines the vector index inline in `CREATE TABLE`, never as a later backfill, to avoid write-locking `memory_chunks`/`promises` during migrations. | Local Docker image `cockroachdb/cockroach:latest-v26.1` (stable current release line; comfortably ≥ v25.2 floor). Enterprise/CCL features (including vector indexing) are free for non-production use under the CockroachDB Community License, which covers this local/demo deployment. |
| CockroachDB Managed MCP | [cockroachlabs.com/blog/cockroachdb-ai-agents-managed-mcp-server](https://www.cockroachlabs.com/blog/cockroachdb-ai-agents-managed-mcp-server/) | 2026-07-29 | Cloud-hosted endpoint (announced 2026-03-25); read-only by default, OAuth 2.1 or service-account API keys; exposes schema inspection, EXPLAIN, read-only SQL. It is a CockroachDB **Cloud** feature — no self-hosted/local equivalent ships. | Not implemented or run in this release. There is no `packages/mcp-bridge` package and no Cloud MCP endpoint configuration. The narration's reference to Managed MCP describes the target design, not demonstrated sponsor usage; the YouTube description discloses this boundary. |
| AWS Bedrock Converse API | [docs.aws.amazon.com/bedrock/.../conversation-inference.html](https://docs.aws.amazon.com/bedrock/latest/userguide/conversation-inference.html), [npmjs.com/package/@aws-sdk/client-bedrock-runtime](https://www.npmjs.com/package/@aws-sdk/client-bedrock-runtime) | 2026-07-20 | `@aws-sdk/client-bedrock-runtime`, `ConverseCommand`, unified message/tool-use interface across model families. | `packages/bedrock` implements `OutreachPlanner` against `ConverseCommand` with tool-use JSON schema for `OutreachPlan`. No AWS credentials exist in this environment, so the fixture adapter (deterministic, evidence-grounded canned plans) is what actually runs; both adapters implement the identical `OutreachPlanner` interface so swapping is a config flag (`BEDROCK_MODE=live|fixture`), not a rewrite. |
| Remotion | [remotion.dev/docs/render](https://www.remotion.dev/docs/render), [remotion.dev/docs/cli/render](https://www.remotion.dev/docs/cli/render) | 2026-07-22 (pinned exact version confirmed via `npm view remotion version`) | `4.0.496` (current stable at pin time), `pnpm exec remotion render <entry> <composition-id> <output>`, Chrome Headless Shell auto-managed. `Config.setPublicDir()`'s `__dirname` resolves to `@remotion/cli`'s own package directory inside the config loader (not the config file's real location) — anchored on `process.cwd()` instead (see `demo/animation/remotion.config.ts`). Frame capture uses `png`, not `jpeg`, because `jpeg` tags output as `yuvj420p` (full-range) instead of the required `yuv420p`. | `demo/animation/` is a Remotion v4.0.496 project; final render via `demo/scripts/render_with_lock.py`, which wraps `pnpm exec remotion render` in the shared `/tmp/hackathon-video-render.lock`. |
| MLX-Audio / Qwen3-TTS | [github.com/Blaizzy/mlx-audio](https://github.com/Blaizzy/mlx-audio), [huggingface.co/mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit](https://huggingface.co/mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit) | 2026-07-20 | CLI: `mlx_audio.tts.generate --model mlx-community/Qwen3-TTS-12Hz-1.7B-Base-8bit --text "..."`; Python API `load_model()` / `model.generate(text=..., voice=..., lang=...)`. Model converted from `Qwen/Qwen3-TTS-12Hz-1.7B-Base` via mlx-audio 0.3.0. | `demo/scripts/narrate.py` uses the Python API scene-by-scene, exact model id pinned, mlx-audio version pinned after smoke test in `docs/BUILD_EVIDENCE.md`. |

## Notes

- No AWS or CockroachDB Cloud credentials exist in this environment (reconfirmed
  2026-07-29: no usable AWS configuration and no Cloud cluster configuration). The entrant
  has since authorized autonomous submission work, but authorization cannot substitute for
  missing accounts, credentials, qualifying-feature execution, or a deployed application.
  The demonstrated release remains local-only: Dockerized CockroachDB, fixture-mode Bedrock,
  and no running Managed MCP endpoint.
- This file will be updated if any source is re-checked closer to the submission deadline.
